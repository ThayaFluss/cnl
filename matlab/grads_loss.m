function [grads,loss] = grad_loss(x,x_dim, LA, sigma,scale,p_dim, dim,f_thres, b_thres,i_grads, i_G)
%GRADS_LOSS Summary of this function goes here
%   Detailed explanation goes here
for i = 1 to x_dim
    z = sqrt(x + sqrt(-1)*scale)
    %%% forward
    while()
    




grads = 1;
loss = 1;
end

